# SRC中文平台｜核心架构冻结规则

你正在维护现有的 SRC 中文平台。

本任务不是重新搭建系统，也不是重新设计 UI。

目标是：在【保持现有系统 UI、页面结构、字体、字号、颜色、按钮样式、布局、交互风格及已有功能不变】的前提下，重新确认、整理并固定现有系统的核心业务架构，作为未来所有功能扩展的统一骨架。

==================================================
一、核心产品流程
==================================================

平台统一采用：

直接测试
→ 初步级别 estimated_level
→ 引导注册
→ 推荐正式 SRC 测试
→ 正式测试
→ 确认级别 confirmed_level
→ 我的中文成长地图
→ 当前级别 + 下一阶段
→ 定制绘本 / 闯关 / 阅读
→ 阅读数据进入成长地图
→ 持续成长

不得创建第二套互相独立的成长逻辑。

==================================================
二、测试体系
==================================================

1. 直接测试 Quick Assessment

作用：
- 面向未注册用户
- 快速体验
- 初步判断孩子的大致 SRC 水平
- 引导家长注册

结果字段：

estimated_level

例如：

estimated_level = SRC300

estimated_level 只能表示"初步预计级别"，不能视为正式级别。

允许：

estimated_level = NULL

不得人为设置默认 SRC 等级。

--------------------------------------------------

2. 正式 SRC 测试

目前正式测试：

SRC100
SRC300
SRC500
SRC800

正式测试用于确认孩子的正式中文水平。

结果字段：

confirmed_level

只有正式 SRC 测试完成后，才能产生 confirmed_level。

默认：

confirmed_level = NULL

不得将 estimated_level 临时复制为 confirmed_level。

==================================================
三、测试状态
==================================================

使用统一状态：

assessment_status

允许：

not_started
estimated
confirmed

含义：

not_started：
尚未完成任何测试。

estimated：
已完成直接测试，但尚未完成正式 SRC 测试。

confirmed：
已完成正式 SRC 测试，已有正式 confirmed_level。

==================================================
四、正式测试推荐
==================================================

直接测试完成后：

estimated_level
→ 后端统一计算
→ recommended_test_level

例如：

estimated_level = SRC300
→ recommended_test_level = SRC300

前端不得自行计算或硬编码该映射规则。

==================================================
五、成长地图
==================================================

"我的中文成长地图"是注册用户的核心成长页面。

核心数据：

current_level
next_level

其中：

current_level 必须来自 confirmed_level。

next_level 必须由后端根据 confirmed_level 通过统一 Level Mapping 计算。

例如：

SRC100 → SRC300
SRC300 → SRC500
SRC500 → SRC800
SRC800 → 后续级别

前端严禁自行计算 next_level。

==================================================
六、Level Mapping 单一来源
==================================================

所有 SRC 等级映射必须只有一个后端权威来源。

包括：

- recommended_test_level
- current_level
- next_level
- 未来内容推荐级别

不得在多个页面、组件、API 或前端代码中重复定义：

SRC100 → SRC300
SRC300 → SRC500
等规则。

前端只负责展示和交互。

后端负责业务规则和计算。

数据库负责保存事实数据。

==================================================
七、定制绘本
==================================================

未来定制绘本的主要难度依据必须是：

confirmed_level

不得直接使用 estimated_level 作为正式绘本难度依据。

基本逻辑：

confirmed_level
→ 绘本难度
→ 定制/推荐绘本
→ 孩子阅读
→ 阅读数据
→ 成长地图

==================================================
八、阅读成长数据
==================================================

成长地图可以记录：

- 当前 SRC 级别
- 下一阶段
- 已完成绘本数量
- 累计阅读字数
- 阅读历史
- 阅读进度

注意：

"识字量"与"累计阅读字数"必须严格区分。

识字水平主要来自正式 SRC 测试。

阅读量来自实际阅读记录。

不得将累计阅读字数直接认定为识字量。

==================================================
九、未来10本免费定制绘本
==================================================

正式测试完成后，未来可向孩子提供10本免费定制绘本。

孩子可以选择主题，例如：

西游记
哈利波特（必须遵守相关版权规则）

绘本难度主要依据 confirmed_level。

完成10本后：

→ 显示更多后续绘本。

该功能可以暂未完全开放，但数据结构和业务逻辑不得破坏上述核心架构。

==================================================
十、数据库与状态规则
==================================================

核心字段必须保持明确语义：

estimated_level
confirmed_level
recommended_test_level
current_level
next_level
assessment_status

不得创建重复含义的 level 字段。

如确需新增 Level 相关字段，必须先说明原因，并确认现有字段无法满足需求。

不得删除、重命名或改变现有核心字段语义，除非明确获得授权。

==================================================
十一、API与前端规则
==================================================

所有 Level 业务计算必须由后端统一完成。

前端：

- 不计算 Level
- 不计算 next_level
- 不计算推荐测试级别
- 不硬编码 SRC 等级映射
- 不建立第二套 Level 状态

前端只负责：

展示
交互
调用 API
显示后端返回结果

==================================================
十二、最小修改原则
==================================================

本任务不是重新开发平台。

必须：

1. 保留现有 UI；
2. 保留现有字体；
3. 保留现有字号；
4. 保留现有颜色；
5. 保留现有布局；
6. 保留现有按钮样式；
7. 保留现有交互风格；
8. 保留已有正常功能；
9. 不进行无必要的 UI 重构；
10. 不重新搭建系统。

只整理、修正和固定核心业务架构。

任何新增功能以后都必须作为现有架构中的模块扩展。

==================================================
十三、身份体系核心规则
==================================================

1. User ID 用于识别登录账户。
2. Child ID 用于识别唯一孩子学习档案。
3. Class ID 用于识别班级。
4. Email 是 User Account 的登录/联系方式，
   不作为 Child 的唯一身份。
5. Parent 登录后，通过 Parent-Child Relation
   获取其有权限访问的 Child ID。
6. Teacher 登录后，通过 Teacher-Class-Child Relation
   获取其有权限管理的 Child ID。
7. 所有测试、Level、阅读、绘本、成长地图数据
   必须统一绑定 Child ID。
8. 一个 Child 可以同时关联 Parent、Teacher 和 Class。
9. Parent 与 Teacher 不得各自创建该 Child 的第二份学习档案。
10. Child 的身份关联与账户登录必须解耦。

==================================================
十四、修改前强制检查
==================================================

收到任何新功能需求后，先检查：

1. 属于哪个现有模块？
2. 使用哪些已有数据？
3. 是否修改数据库 Schema？
4. 是否新增重复字段？
5. 是否影响 estimated_level？
6. 是否影响 confirmed_level？
7. 是否影响 recommended_test_level？
8. 是否影响 current_level？
9. 是否影响 next_level？
10. 是否存在前端 Level 硬编码？
11. 是否破坏 API 与数据库向后兼容？
12. 是否改变现有 UI？
13. 是否违反身份体系核心规则？
14. 是否绕过 Child ID 直接绑定 User ID？
15. 是否会造成重复 Child 档案？

如果与核心架构冲突：

不得自行修改架构。

必须先报告：

【冲突位置】
【冲突原因】
【建议方案】

等待确认后再修改。

==================================================
十四、本次任务执行方式
==================================================

现在不要重新开发系统。

不要重新设计 UI。

不要新增功能。

不要主动迁移数据库。

不要删除代码。

请先对现有代码进行 Architecture Audit：

1. 找出所有 Level 相关字段；
2. 找出所有 Level 计算逻辑；
3. 找出所有 Level API；
4. 找出前端状态管理中的 Level；
5. 找出重复定义；
6. 找出与本架构冲突的地方；
7. 检查数据库 Schema；
8. 检查路由/API；
9. 检查前端状态管理；
10. 检查现有 UI 是否会受到影响。

输出：

《SRC核心架构冲突审计报告》

报告完成前不要修改代码。

==================================================
最终原则
==================================================

【不重建系统】
【不重做UI】
【不改变现有视觉设计】
【不破坏已有功能】
【后端统一业务规则】
【前端只负责展示与交互】
【Level只有单一事实来源】
【模块化扩展】
【最小修改】
【先审计，后修改】

以后所有 SRC 中文平台功能，都必须建立在以上核心骨架之上。
